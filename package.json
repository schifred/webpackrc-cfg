{
  "name": "webpack-config",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "test": "dn test && dn build -e prod",
    "build": "rimraf lib && babel src --out-dir lib"
  },
  "author": "alfred.qiu",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "debug": "^4.0.1",
    "os": "^0.1.1",
    "shify": "^3.0.6"
  }
}
